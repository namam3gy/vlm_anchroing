seed: 42
output_root: outputs

vqa_dataset:
  local_path: inputs/plotqa_test
  answer_range: 10000        # integer GT in [1, 10000]
  samples_per_answer: 5      # cap per GT class to keep distribution diverse
  max_samples: 200
  require_single_numeric_gt: true

inputs:
  irrelevant_number_dir: inputs/irrelevant_number
  anchor_sampling: stratified
  anchor_distance_scheme: relative   # per-GT hybrid scale-relative cutoffs (wide GT)

models:
  - name: llava-next-interleaved-7b
    hf_model: llava-hf/llava-interleave-qwen-7b-hf

sampling:
  temperature: 0.0
  top_p: 1.0
  max_new_tokens: 16

prompt:
  system: |
    You are a visual question answering system.
    Output a single number only. No words, no units, no explanation, no markdown.
    If uncertain, still output the single most likely number.
  user_template: |
    Answer the question using the provided image(s).
    Output a single number.
    Question: {question}
